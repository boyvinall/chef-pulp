# This file is managed by Chef,
# and generated for <%= node[:fqdn] %>

# =========================
# Pulp Server Configuration
# =========================

# -- Common Configuration -----------------------------------------------------

<% node['pulp']['server']['conf'].each do |block, config| %>
[<%= block %>]
<% config.each do |key, value| %>
<%= key %>: <%= value %>
<% end %>

<% end %>
